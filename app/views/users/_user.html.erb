<article class="user-in-list">
	<%= gravatar_for user %>
	<h2><%= link_to user.name, user_path(user) %>
		<% if current_user && current_user.admin? && !user.admin? %>
			<small>
				<%= link_to 'delete user', user, method: :delete %>
			</small>
		<% end %>
	</h2>
</article>